export default class Client {
  constructor(id, nom, prenom, telephone, adresse, point, photo) {
    this.id = id;
    this.nom = nom;
    this.prenom = prenom;
    this.telephone = telephone;
    this.adresse = adresse;
    this.point = point;
    this.photo = photo;
  }
}
